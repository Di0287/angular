import { ClueDirective } from './clue.directive';

describe('ClueDirective', () => {
  it('should create an instance', () => {
    const directive = new ClueDirective();
    expect(directive).toBeTruthy();
  });
});
